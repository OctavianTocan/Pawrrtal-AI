/**
 * Placeholder for future `GET /api/v1/models` integration via React Query.
 *
 * @fileoverview Currently unused; reserved so model picker can load server-driven options.
 */

// import { useAuthedQuery } from "@/hooks/use-authed-query";
// import { API_ENDPOINTS } from "@/lib/api";

// export default function useModels() {
// 	//TODO: This is going to be used to fetch the models from the backend.
// 	return useAuthedQuery<>(
// 		["models"],
// 		API_ENDPOINTS.chat.models,
// 	);
// }
