# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules/
/.pnp
.pnp.*
.venv/
.yarn/*
.mypy_cache/
.pytest_cache/
.ruff_cache/
.vscode/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
!.env.example
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Private
/private
.private

# testing & coverage
/coverage
coverage/
**/coverage/
.nyc_output/
*.lcov
.vitest-cache/
htmlcov/
.coverage
.coverage.*
junit.xml
test-results/
playwright-report/
# Stagehand E2E auxiliary artifacts:
#   - `.trace-extract/` is the temp dir created by the GIF afterEach
#     hook in `frontend/e2e/stagehand/fixtures.ts` to unzip trace.zip.
#   - `**/run.gif` is the per-test GIF artifact (also lives under
#     test-results/, but pattern protects against stragglers).
#   - `.stagehand-cache/` is NOT ignored — it's committed so CI shares
#     the warm Stagehand action cache (per the suite's README).
**/.trace-extract/
**/run.gif

# next.js
/.next/
/out/

# production
build/
cache/
dist/

# Next
.next/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# logs (including rotated logs like app.log.1, app.log.2.gz, etc.)
*.log
*.log.[0-9]*
*.log.[0-9]*.gz

# env files (can opt-in for committing if needed)
.env*
!.env.example

# repomix output files
repomix-output.*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Python
__pycache__
*.py[cod]
*$py.class
*.egg-info/
.tox/
.pyre/
.pytype/
pip-log.txt
pip-delete-this-directory.txt

# Database
agno.db
agno.db-shm
agno.db-wal

# sentrux local baseline (per-developer, not shared)
.sentrux/baseline.json
